import java.util.*;

public class DeerInZooDivTwo {
    public int[] getminmax(int N, int K) {
        int max = K / 2;
        if (K % 2 == 1)
            max++;

        return new int[]{Math.max(0, N - K), N - max};
    }
}
